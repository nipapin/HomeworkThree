<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".circleActivity" android:orientation="vertical" android:id="@+id/circleScreen">

    <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="numberDecimal"
            android:ems="10"
            android:id="@+id/radius"
            android:layout_margin="12sp" android:gravity="center" android:hint="input radius"/>
    <Button
            android:text="@string/squareButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:id="@+id/getSButton"
            android:elevation="0dp"
            android:layout_margin="12sp" style="@style/Widget.AppCompat.Button.Borderless.Colored"/>
    <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:id="@+id/areaResult"
            android:textSize="24sp"
            android:layout_margin="12sp" android:gravity="center" android:hint="@string/squareSign"/>
    <Button
            android:text="@string/lengthButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:id="@+id/getCButton"
            android:layout_margin="12sp" style="@style/Widget.AppCompat.Button.Borderless.Colored"
    />
    <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:id="@+id/perimResult"
            android:textSize="24sp"
            android:layout_margin="12sp" android:gravity="center"
            android:hint="@string/perimeterSign"/>
</LinearLayout>